# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Node (only used for optional tooling)
node_modules/

# recall captured data — lives in ~/.recall, NEVER in the repo
.recall/
raw/
cmd/
meta/
sessions/
*.jsonl
*.log

# diagram source — only the exported PNG (docs/architecture.png) is committed
*.drawio
*.drawio.bkp

# secrets / auth — NEVER commit
.env
.env.*
*.pem
*.key
secrets/
