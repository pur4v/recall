# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Node (only used for optional tooling)
node_modules/

# recall captured data — lives in ~/.recall, NEVER in the repo
.recall/
raw/
cmd/
meta/
sessions/
*.jsonl
*.log

# secrets / auth — NEVER commit
.env
.env.*
*.pem
*.key
secrets/
